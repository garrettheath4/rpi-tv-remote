{
  "name": "webapp",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "react-scripts build && zip -r build build && echo 'Login to copy build files to RPi' && scp build.zip pi@LobbyNews.local:~/tv-remote/webapp/ && echo 'Login to extract build files on RPi' && ssh pi@LobbyNews.local 'cd ~/tv-remote/webapp/ && unzip -uo build'"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
